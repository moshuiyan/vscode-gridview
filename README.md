# vscode-gridview

Lightweight extraction of VS Code GridView / SplitView / Sash.

Build

1. npm install
2. npm run build

Demo

- After build, open demo/index.html (not included yet) to try a simple example.
